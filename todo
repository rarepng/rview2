fml
OBS HEADLESS!!!!!!!!
bendy bones
gns piping
viseme mapping
modularize passes (compute: pre, post, async)
miniaudio
the stupid vertex smearing
kill the bucket
unify cleanup as lambdas (more memory but cleaner)
texture compression at runtime [droptime] (probably basis universal)
wuffs/exr/replace stb (except maybe for hdr)/that gltf extension for compressed images
single source for slang and cpp
PAINFUL order independant transparency
refractions and SSR
inverse hull outlines (somehow efficiently)
gltf compliance
gltf extensions (emission, sheen, gloss)
PROPER DELETION and DEFERRED CLEANUP!!!!!! [[CRITICAL]] // maybe dedicated wait device idle lambda?
volk for runtime recovery
sparse virtual textures because $*@# id
actual ui overhaul with proper tools
shadows [i dont care for shadows personally but gotta rip that bandaid off at some point]
anti aliasing [ ditto ]
decoupled physics loop(s)
replace imgui or find a way to use it without all the vertex overhead
tune VMA to use maybe 64MB instead of 256MB
runtime env map update (and skybox)
clang-format instead of astyle [maybe]
update readme
demos!!!!!!!
jolt
meshoptimizer
hot reloading dll (separate for dev cycle)
hot reloading shaders
apple clang preset(s) X_X [probably never let's be real]
